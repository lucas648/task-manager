import { Routes } from '@angular/router';
import { ContentReviewComponent } from './features/content-review/content-review.component';

export const routes: Routes = [
  {
    path: '',
    component: ContentReviewComponent,
  },
];